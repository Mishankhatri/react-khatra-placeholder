/**
 * PCardContainer is container to hold one or more PCard.
 * @param {*} { children }
 * @return {*}
 */
const PCardContainer = ({ children }) => {
  return <div className="p-card-container">{children}</div>;
};

export default PCardContainer;
